import 'package:flutter/material.dart';
import 'package:flutter_webview_plugin/flutter_webview_plugin.dart';

class paychpa extends StatefulWidget {
  const paychpa({Key? key}) : super(key: key);

  @override
  State<paychpa> createState() => _paychpaState();
}

class _paychpaState extends State<paychpa> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
    );
  }
}
